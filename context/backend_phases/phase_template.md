# Phase N — {제목}

> 새 phase 문서 작성 시 이 파일을 복사해서 시작. 각 섹션은 필요 시 생략 가능하나 헤딩 순서는 유지.

## 목표

이 phase에서 무엇을 완성하는지 1~2문장. 어떤 컴포넌트가 머지 후 사용 가능해지는지 명시.

## 적용 패턴

이 phase에서 도입/사용하는 GoF / 아키텍처 패턴:

- **{패턴명}**: 어디에 어떻게 적용했는지 한 줄
- ...

## 구성요소

각 클래스/모듈 단위로 서브섹션. 의존성 적은 것부터.

### `{클래스명}`

책임 1~2문장.

```
// 핵심 멤버 / 메서드 시그니처. 구현 디테일이 아닌 인터페이스 위주
field_: Type
method(args) → ReturnType
```

판단/근거가 필요한 설계 결정은 bullet으로:
- 왜 이렇게 했는지
- 대안 vs 선택 이유

### `{다른 클래스명}`

...

## 의존성

- 상위: 이 phase가 의존하는 이전 phase 컴포넌트
- 하위: 이 phase를 의존할 후속 phase

forward declaration / setter 주입 등 컴파일 단위 트릭이 있다면 명시.

## 테스트

`tests/phase_N_*.cpp` 파일별 검증 항목:

- **{컴포넌트}**: 케이스 1, 케이스 2, ... (mock 사용 여부 명시)
- **{컴포넌트}**: ...

엣지 케이스 / 회귀 방지 포인트도 포함.

## 산출 브랜치

`back/feat/{name}` (또는 분할 시 복수 브랜치 나열)

## 후속

이 phase에서 미해결로 남긴 / 후속 phase로 미룬 결정:
- 항목 1 → 어느 phase에서 확정
- ...
